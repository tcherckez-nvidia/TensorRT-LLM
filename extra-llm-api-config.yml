max_num_tokens: 80000
kv_cache_config:
  dtype: fp8
  enable_block_reuse: false
  free_gpu_memory_fraction: 0.8
  mamba_cache: fp16
moe_config:
   backend: TRTLLM
cuda_graph_config:
    enable_padding: true
    max_batch_size: 256
enable_attention_dp: false
enable_chunked_prefill: false
num_postprocess_workers: 4
print_iter_log: true
stream_interval: 10
